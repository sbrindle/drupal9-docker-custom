memory_limit = 512M
max_execution_time = 60
max_input_vars = 2000

# Increase to 50 to allow multiple files upload.
post_max_size = 50M

# Hide PHP version in HTTP headers
expose_php = Off

# Cookies
session.cookie_secure=1
session.cookie_samesite="Lax"

# Upload max size
upload_max_filesize = 10M
post_max_size = 50M
